import React from "react";

const WALL_SHADOW_COLOR = "bg-black";
const WallShadow = () => {
	return (
		<div className={`${WALL_SHADOW_COLOR} w-full h-full opacity-30`}></div>
	);
};

export default WallShadow;
